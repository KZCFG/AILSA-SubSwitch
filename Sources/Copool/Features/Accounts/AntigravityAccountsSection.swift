import SwiftUI

// Retained as an empty source file while older Xcode project metadata is
// regenerated. The old standalone AntiGravity section had its own account
// store, so it must not be rendered beside the unified provider picker.
