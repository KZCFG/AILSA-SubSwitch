import Foundation

// AntiGravity now shares `AccountsPageModel` and `AccountsCoordinator` with
// Codex. This file intentionally remains as a project-file compatibility shim
// while the former parallel account list and coordinator are retired.
