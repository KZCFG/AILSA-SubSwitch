# Single source of truth for the shipped version.
# Consumed by scripts/build_app.sh and scripts/sync_version.sh; project.yml and
# AILSA_SS.xcodeproj are derived from these values (run scripts/sync_version.sh).
# Feature milestones change MARKETING_VERSION; maintenance revisions append
# (a), (b), (c), ... in BUILD_LABEL. BUILD_NUMBER remains YYYYMMDDNN for every build.
MARKETING_VERSION=1.0.0
BUILD_NUMBER=2026092302
BUILD_LABEL=1.0.0(c)
